name: Simple Workflow

on:
  push:
    paths: 
    - 'hello.sh'
    
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, pushed hello.sh"
        


